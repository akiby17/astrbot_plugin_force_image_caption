# Changelog

## 1.2.4 - 2026-09-09

### RPM / TPM 防护优化

- **自然续聊不再重复识图**：发图后继续说“哈哈”“确实”“这个好可爱”等普通续聊时，优先复用上一轮成功的图片转述，不再每句话都重新调用视觉模型。
- **细节追问功能保留**：像“第二个是谁”“右边的人在干嘛”“图里写了什么”“两张有什么区别”等需要新视觉信息的问题，仍会重新查看原图，不牺牲跨消息图片追问能力。
- **新增转述缓存**：相同图片 + 相同问题在短时间内可直接命中缓存，减少 AstrBot 重复事件或相同请求造成的额外 API 调用。
- **新增并发去重**：同一图片请求已经在识别时，后续相同请求共享正在执行的任务，不再重复创建视觉请求。
- **新增视觉请求串行保护**：默认将视觉请求依次发送，降低群聊或插件并发造成的瞬时 RPM 峰值。
- **新增 Provider 熔断/冷却**：检测到 `429`、`rpm exhausted`、TPM 超限、配额耗尽、账号计费暂停或鉴权异常后，默认冷却 60 秒；冷却期间优先复用已有图片转述，不继续撞 API。

### 重试策略优化

- 新增 `enable_plugin_retries` 总开关，默认关闭；即使从 v1.2.2 升级后旧配置里仍保存 `max_retries=2`，也不会继续“双层重试”。
- `max_retries` 默认由 **2 改为 0**。只有手动开启插件额外重试后该数值才生效。
- 保留插件重试配置，旧 Provider 或特殊场景仍可手动设为 1～2 次，不删除原有能力。
- **429/403/计费暂停等硬错误立即停止插件级重试**，不再进行无意义重复请求。
- **多图逐张恢复保留但改为智能触发**：只有批量格式、图片数量、payload/400/413/422 等可能通过拆图解决的问题才逐张尝试；RPM/TPM、计费暂停、超时、连接异常和 5xx 不会拆成更多请求。
- 多图逐张恢复过程中一旦遇到 429/计费/鉴权类硬错误，立即中止剩余图片，防止一次失败扩散成多次 API 调用。

### 缓存与状态

- 最近图片记忆现在同时保存最近一次成功转述；同一图片被 AstrBot 多个阶段重复观察时会保留该转述。
- AstrBot 已经生成 `<image_caption>` 时，插件除了直接复用，还会把该结果写入最近图片缓存，供下一轮自然续聊直接使用。
- `/force_caption_status` 新增 v1.2.4 防 RPM 状态：转述复用、串行保护、冷却时间、当前冷却剩余时间以及本会话是否已有可复用转述。

### 新增配置

- `reuse_caption_for_natural_followup`：自然续聊优先复用上一轮转述，默认开启。
- `enable_plugin_retries`：插件额外重试总开关，默认关闭。
- `caption_cache_ttl_seconds`：图片转述缓存时间，默认 300 秒。
- `serialize_caption_requests`：视觉请求串行保护，默认开启。
- `rate_limit_cooldown_seconds`：429/计费拒绝后的冷却时间，默认 60 秒。

## 1.2.2 - 2026-09-06

### 修复

- 修复用户先发图片、随后用自然口语继续聊时，主模型仍会追问“发了什么图/是什么东西”的问题。
- v1.2.1 只会在“第二个是谁”“这张呢”“图里是什么”等明显图片追问时复用最近图片；像“这个超级好吃”“好想来买的”“笑死”“也太可爱了”这类陈述式续聊可能不会命中。
- 现在新增**短时自然续聊图片语境**：发图后的一小段时间内，同一发送者继续聊天，即使没有明确提到“图片”，也会自动复用最近图片并调用视觉模型。
- “这个/那个/这玩意/那东西/它/他/她”等短句式自然指代也会直接识别为图片追问。

### 群聊防串图

- 自然续聊默认仅对**发图本人**生效，避免多人群聊里其他人突然说一句普通话时误带入上一位群友的图片。
- 明确的图片追问（如“这张是什么”“第二个是谁”“图里写了啥”）仍可在最近图片 TTL 内正常复用。

### 新增配置

- `natural_followup_enabled`：启用图片后的短时自然续聊，默认开启。
- `natural_followup_window_seconds`：自然续聊图片语境窗口，默认 90 秒。
- `natural_followup_same_sender_only`：自然续聊是否仅限发图本人，默认开启，群聊建议保持开启。

## 1.2.1 - 2026-08-31

### 修复

- 修复 AstrBot 临时图片位于 `data/temp` 时，后续 LLM 工具调用执行较晚导致原文件已被清理、出现“图片文件不存在”的问题。
- 新增**临时图持久化**：收到本地临时图片后，会复制到插件自己的 `data/plugin_data/astrbot_plugin_force_image_caption/recent_images/` 缓存目录。
- 持久化时保留原始文件名，并尽量改写当前消息中的 `Image.url/file/path`。这样 `astrbot_plugin_stealer` 的 `steal_meme` 即使拿到已经失效的旧临时路径，也可以按 basename 从当前消息中解析到仍然存在的持久副本。
- 修复 `ProviderRequest.image_urls` 仍残留失效临时路径时可能拖累视觉模型请求的问题：会优先用当前消息中同名的持久副本替换，无法替换的失效本地路径会被丢弃。
- 最近图片跨消息记忆现在优先保存持久副本，不再依赖 AstrBot 临时目录的文件生命周期。

### 新增配置

- `persist_recent_images`：是否把本地临时图片复制到插件持久缓存，默认开启。
- `persistent_cache_dir`：自定义持久缓存目录，默认留空并使用 AstrBot `data/plugin_data`。

### 清理策略

- 持久缓存会按最近图片 TTL 加安全宽限时间自动清理，避免长期运行无限积累。
- `/force_caption_status` 增加“临时图持久化”状态。

## 1.2.0 - 2026-08-31

### 新增

- 新增**最近图片会话记忆**：即使图片消息本身没有触发 LLM，也会监听并缓存图片引用，支持下一条消息继续追问。
- 新增**跨消息图片追问**：支持“第二个是谁”“左边是谁”“这张呢”“什么意思”等常见连续问法。
- 新增**问题感知识图**：调用视觉模型时会附带当前用户问题，让识图结果优先提供回答当前问题所需的信息，例如人物顺序、位置、动作、文字等。
- 新增 `/force_caption_forget` 命令，可手动清除当前会话的最近图片记忆。
- `/force_caption_status` 增加最近图片记忆、TTL、复用模式和当前缓存状态显示。
- 新增最近图片相关配置：记忆开关、有效期、仅追问时复用、单会话最大图片数、问题文本最大长度。

### 优化

- 当前消息已经被 AstrBot 成功转述时，也会先记录原图，保证下一轮仍能继续追问。
- 最近图片默认仅在“疑似图片追问”时复用，避免把旧图误带入普通聊天。
- 最近图片缓存只保存 URL/文件路径等轻量引用，不保存 `data:` Base64 大对象，降低长时间运行时的内存占用。
- 缓存增加过期清理，并限制单会话最多保存的图片数量。
- 调试日志新增图片来源（current/recent）、缓存命中与缓存年龄信息。
- 视觉模型提示词更贴近聊天场景，减少泛泛描述，提升针对当前追问的有效信息密度。
- 疑似图片追问但当前图和最近图都不可用时，也会注入静默约束，尽量避免主模型直接暴露“看不到图片/识图失败”等内部状态。

### 保持兼容

- 保留 v1.1.0 的当前图、引用图、多图、自动重试、多图拆分恢复、空响应重试、失败标记清理与静默降级能力。
- 默认仍适用于 AstrBot `>=4.23,<5`。

## 1.1.0

- 支持当前图片、引用图片和多图自动转述。
- 增加识图失败重试、指数退避、超时控制与空响应重试。
- 增加多图整体失败后的逐图恢复。
- 增加静默失败处理，避免把 `[Image Captioning Failed]` 等内部状态暴露给主聊天模型。
